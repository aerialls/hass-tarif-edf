# Workflow

- Never push commits directly to `main`. Always create a feature branch, push it, and open a pull request for review instead.
- Before opening (or updating) a pull request, rebase the branch onto the latest `main` first.
